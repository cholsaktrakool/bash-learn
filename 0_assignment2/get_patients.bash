#!/usr/local/bin/bash

# cat patients.txt | awk '{print $1}'

# cat patients.txt | grep -i bangkok > output.txt

# cat patients.txt | grep -i bangkok | wc -l

# [[ ! -d ./output/ ]] && mkdir output



